# acceptance-gate

Claude Code reads `CLAUDE.md`, not `AGENTS.md`. This file exists so that the
routing table and the glossary are both loaded; deleting it as redundant would
silently drop them from every session.

@AGENTS.md
@CONTEXT.md
