# bddgen output — regenerated by `test:e2e` on every run
.features-gen/

# Playwright run artifacts
playwright-report/
test-results/

# The visual-diff worlds, rebuilt by `scripts/seed-visual-diff.mjs` on every boot
.worlds/
